<ul id="grid">
  <% @categories.each do |category| %>
    <li>
      <%=link_to image_tag("imagegrid_files/icon-menu-red.png", :alt=>category.name, :border=>0), category_products_path(category)%>
    </li>
  <% end %>
</ul>
